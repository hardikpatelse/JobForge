# JobForge UI (Angular 20)

- Standalone Angular 20 app with Tailwind.
- API access must go through APIM; no direct service URLs.
- Use environment-based configuration; no hardcoded URLs.

Run (placeholder):
- npm install
- npm run start
