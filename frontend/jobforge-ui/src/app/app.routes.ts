import { Routes } from '@angular/router';

export const routes: Routes = [
  // TODO: Add routes for job search, job details, user profile, etc.
  // Example:
  // { path: '', redirectTo: '/jobs', pathMatch: 'full' },
  // { path: 'jobs', loadComponent: () => import('./features/jobs/jobs.component').then(m => m.JobsComponent) },
  // { path: 'jobs/:id', loadComponent: () => import('./features/job-details/job-details.component').then(m => m.JobDetailsComponent) },
  // { path: 'profile', loadComponent: () => import('./features/profile/profile.component').then(m => m.ProfileComponent) },
];
